*.log
.DS_Store
/vms/
.claude/
.env
.env.*
!.env.*.example

# Skill repos — pulled by ./refresh-skills.sh, not committed (their own .git dirs
# would otherwise turn into half-broken submodules).
/skills/evm-audit-skills/
/skills/pashov-skills/

# generated pretty reports (published to leftclaw-services, not here)
scripts/report/out/
